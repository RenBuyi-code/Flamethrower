# AI.md

本文件定义 `Flamethrower` 工程的 AI 协作规范。任何 AI 在本仓库进行代码设计、修改、重构、测试时，必须遵守。

## 1. 项目目标
- 在现有 FreeRTOS 基础上，实现可靠、安全、可移植、可扩展的喷火机控制系统。
- 安全优先级永远高于功能完整度与性能优化。

## 2. 需求基线（唯一事实来源）
- `doc/大喷火机客户给的需求.md`
- `doc/DMX控制图.png`（DMX 本机受控方式唯一数据源）
- `doc/两通道模式.csv`（DMX 图的转写副本，仅用于辅助核对）
- `doc/六通道模式.csv`（DMX 图的转写副本，仅用于辅助核对）
- `doc/工程TODO_AI执行版.md`
- `doc/工程长任务TODO_REVIEW.md`

若实现与理解冲突：以上文档优先，其次才是历史代码行为。

DMX 特别规则：2CH/6CH 通道含义、DMX 值阈值、点火时间和泄压/急停窗口必须以 `doc/DMX控制图.png` 为唯一判定来源；CSV/Markdown 若与图片冲突，以图片为准，并同步更新转写文档。

## 3. 架构约束（必须）
- 采用分层：`app/`、`domain/`、`hal_if/`、`bsp/`、`cfg/`。
- `domain` 禁止直接依赖芯片寄存器、AT32 驱动类型、GPIO 宏。
- `bsp` 负责硬件细节；`hal_if` 提供抽象接口；`domain` 只写业务与策略。
- 仅 `actuator_task` 允许直接改执行器输出状态（油泵/锁油阀/泄压阀/点火器）。
- 引脚定义唯一来源：`project/inc/at32f415_wk_config.h`。
  - 禁止在其他 `.c/.h` 中重复 `#define` 引脚号/端口号。
  - 若硬件引脚变更，先改 `at32f415_wk_config.h`，再适配 `bsp` 逻辑。

## 4. 安全硬约束（必须）
- 任何异常场景必须快速降级到安全态（`SAFE_OFF`）。
- 点火必须满足全部门控条件：
  - USER MODE
  - 非故障锁存
  - 倾斜正常（或显式关闭倾斜保护）
  - 电压正常
  - 压力在允许区间
  - DMX 命令有效且未超时
- 故障码 E1~E5 必须可追踪、可锁存、可按规则恢复。
- DMX 失联超时必须触发停火/安全处理，不得保持危险输出。

## 5. 并发与 RTOS 规则
- ISR 只做最小工作：采样、缓存、通知；禁止复杂业务逻辑。
- 任务间通信必须使用 FreeRTOS 原语：Queue/EventGroup/StreamBuffer/Timer。
- 禁止用无保护全局变量跨任务共享状态。
- 关键任务必须有心跳；看门狗策略必须能覆盖关键任务失活。

## 6. 编码规范（项目内）
- 语言：C（可 C 风格面向对象：结构体 + 接口函数 + 上下文）。
- 默认静态分配，禁止随意引入动态内存。
- 所有外部输入必须做边界校验：DMX 值、ADC 原始值、菜单参数。
- 参数持久化必须带版本与完整性校验（如 CRC），坏数据回退默认值。
- 不得为“看起来能跑”绕过安全链路。

## 7. 目录与三方代码边界
- `project/`, `app/`, `doc/` 为本项目主要可演进区域。
- `libraries/`, `middlewares/` 默认视为三方依赖，非必要不改。
- `middleware/SlateUI/` 非本次核心安全路径，除 UI 集成需要外不主动改动。

## 8. AI 执行流程（建议）
1. 先读需求基线与 TODO，优先查看 `doc/工程长任务TODO_REVIEW.md` 中的 P0/P1 项。
2. 明确修改影响的状态机、安全链路、任务通信。
3. 优先实现最小安全闭环，再扩展功能。
4. 每次改动后自检：
   - 是否破坏安全降级路径？
   - 是否违反分层？
   - 是否引入并发竞态？
5. 执行强制 Review（见第 11 节），未通过不得交付。
6. 输出时必须说明：改了什么、为什么安全、如何验证、Review 结论。

## 9. 完成定义（DoD）
- 功能满足文档需求（含 2CH/6CH、TEST/USER、E1~E5）。
- 任意故障路径可进入安全态且可追踪。
- 新增功能不破坏分层与可移植性。
- 板卡迁移时主要替换 `bsp/<board>` 与 `cfg/board_profile_*`。

## 10. 禁止事项
- 禁止在 `domain` 直接写寄存器。
- 禁止多个任务直接操作执行器 GPIO。
- 禁止在 ISR 中执行状态机。
- 禁止绕过故障锁存直接恢复喷火能力。
- 禁止提交未经约束校验的危险默认参数。

## 11. 强制 Review Gate（必须）
- R01: 任何代码改动完成后，必须执行一次“安全与并发优先”的自我 Review。
- R02: Review 输出必须至少包含：
  - 改动文件清单
  - 风险点清单（若无则明确写“未发现高风险问题”）
  - 需求映射核对（对应哪条需求/TODO）
  - 测试与验证结果（已做/未做及原因）
- R03: 若存在以下任一项，结论必须为“未通过”，不得宣称完成：
  - 未验证安全降级路径
  - 执行器并发写口未收敛到 `actuator_task`
  - ISR 包含业务逻辑
  - 外部输入缺少边界校验
  - DMX 失联策略缺失
- R04: 若工具或环境限制导致无法完成测试，必须在交付中显式标注“残余风险”。
- R05: 未给出 Review 结论（通过/未通过）视为未完成交付。
